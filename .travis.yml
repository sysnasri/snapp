sudo: required
language: go
os: linux
dist: xenial
services: 
   - docker
before_install:
   - docker build -t nasri/snapp:v1 -f apps/v1.0.0/Dockerfile .
deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master  
    
